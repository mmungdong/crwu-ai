# WorkBuddy configuration

Place sanitized WorkBuddy MCP configuration examples in this directory. Use an
`*.example.json` suffix for committed examples; real `*.json` files are ignored.

Examples must use placeholder values and must not include credentials, tokens,
private endpoints, cookies, or session data.
