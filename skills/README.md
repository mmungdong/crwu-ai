# Python Skills

Python Skills live here as independent deliverables. Each Skill should own its
`SKILL.md`, `pyproject.toml`, source, tests, and usage documentation when it is
introduced.

Skill dependencies are not installed by the root Go build and must not become
runtime dependencies of the `crwu` binary.
