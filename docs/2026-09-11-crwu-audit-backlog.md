# crwu-audit 技能族待办清单（backlog）

> 生成：2026-09-11；依据：最新知识库目录（`fetched_at=2026-09-11T14:10:34+08:00`，297 节点）核对 +
> 本轮资产轴补齐后余留缺口 + `docs/review-crwu-audit-skills.md` §8 问题索引中仍有效的项。
> 用途：把「本轮未做、后续有时间再补」的结构/内容/契约项沉淀在此，逐项带 现状 / 动作 / 依据。
> 事实源仍以最新目录 + `07-skill-registry.md` + 真实技能目录为准；本文件只做待办索引，不替代运行时事实。

---

## P0 · 方法轴落地（资产评估审核命门，知识库内容已齐备、只缺技能）

- **现状**：`method` 轴 7 个标签全 `pending`（资产基础法 / 成本法 / 市场法 / 收益法 / 假设开发法 / 基准地价系数修正法 / 路线价法），`其他方法` profile-only。除房地产对象侧一个「过渡性登记 `VALUATION_METHOD_INTERFACE`」捎带方法准则层外，其余对象（机器设备 / 无形资产 / 企业价值 / 矿业权…）一旦命中收益法/成本法，方法层空转。
- **知识库现状**：`03-评估方法/` 内容已相当完整——`01-市场法` / `02-收益法` / `03-资产基础法` 各「01-定义与适用前提 / 02-具体方法与计算 / 03-常见错误与审核要点」+ `04-方法选择` + `05-审核要点` + `00-评估方法准则2019-精编`（`RULE-01-02-281~305`）。
- **动作**（归口 `crwu-audit-skill-maintainer`，`create` 模式）：
  1. 建 `crwu-audit-method-*`，一级根 `03-评估方法/`（方法准则层为其一级共用层）；
  2. 同时认领 `06-规则库/M-收益法/04-模块-收益法`、两个旗舰清单 `06-规则库/清单-M-市场法/不动产-房产-市场法租金比较-报告审核`（CHK-MKT-001~014）、`06-规则库/清单-M-成本法/不动产-房产-成本法-报告审核`（CHK-CST-001~012）；
  3. 方法轴落地后立即剥离 `crwu-audit-asset-realestate/references/01-kb-assembly.md` §3 的过渡登记 `VALUATION_METHOD_INTERFACE`。
- **依据**：`03-评估方法/` 目录已具备全部正文；review §2.2 问题 3（P1-5）已指出 registry 标签与目录语义需对齐（成本法/假设开发法/基准地价修正/路线价法无对应目录，`04-方法选择`/`05-审核要点` 有目录无标签）。

## P0 · 监管轴落地（先补知识库内容，再建技能）

- **现状**：`overlay` 轴 4 个标签全 `pending`（国资 / 证券 / 司法 / 金融/银行）。
- **知识库现状**：`04-监管覆盖/` 5 个目录（司法 / 国资 / 证券 / 财务报告 / 金融国资）**每个只有一个 README，无审核文件**。
- **动作**：① 先回补知识库 `04-监管覆盖/` 正文（属知识库维护，不在本仓执行）；② 再建 `crwu-audit-overlay-*`。
- **语义待对齐**：registry 标签「金融/银行」vs 目录「金融国资」；目录有「财务报告」但 registry 无对应 overlay 标签。

## P1 · 资产轴对象深度补齐（知识库侧内容工作）

- **现状**：12 个一级资产里，6 个只有 `01-共性参考/01-评估审核条目`、无 `02-细分对象/`：机器设备 / 存货 / 债权 / 资产组合 / 资产组-含商誉 / 废旧物资 / 其他（对比房地产 6 / 无形资产 7 / 企业价值 2 / 矿业权 2 / 交通运输设备 2 有细分对象）。
- **动作**：为上述对象补 `02-细分对象/<对象>/评估审核条目`；补齐后父叶子二级索引自动受益，不另建技能。

## P1 · 业务轴内容补全（知识库侧内容工作）

- **5 个子业务必检项「待补」**：`01-业务路线/02-交易与处置/拍卖与处置`、`…/转让与出售`、`04-融资与债务/ABS及发债`、`05-投资与资本运作/并购与重组`、`…/股权转让` 的 `01-业务通用审核要点`。
- **2 个空文档**：`01-业务路线/01-资产经营/租赁与租金评估/02-方法适用索引`、同目录 `03-监管适用索引`。
- 已按治理如实记 gap、未补造；补齐后回写对应 `crwu-audit-biz-*/references/02-review-focus.md` 的「待补」声明。

## P1 · 历史高频复核问题的频次/项目数信号未进技能（归口 `crwu-audit-optimize`）

- **现状**：知识库 24 个子业务要点合计 560 条历史高频复核问题，每条自带「出现次数 · 涉及项目数 · 分类标签」三个量化字段；叶子在 `02-review-focus.md` 只保留了「分类标签」，**丢弃了次数/项目数**——失去了排序与分诊信号（先查高频、后查低频）。
- **动作**：在叶子保留「高频 TOP-N + 次数」摘要层，属内容层改动。

## P1 · 叶子契约实现缺口（review P1-6）

- `selected_relative_paths` 9 个叶子 0 命中；输出自检（§11）0 命中；三分区（报告/评估说明/测算明细表）业务叶子缺失；asset 叶子「一级共用层先行」缺失；悬空键 `REAL_ESTATE_OBJECT`（12 次，`01-kb-assembly.md` 未登记）；asset 叶子 `expected_structure` 用了非字面节点名；叶子复述共同规则。
- **动作**：对照 `12-leaf-common-contract.md` §5/§6/§11 逐条补位，asset 叶子按 biz 叶子版式收敛。

## P1 · 叶子 → AuditResult 字段映射缺失（review P1-7）

- 三处字段名互不一致：契约 §7（`status`/`report_evidence`/`rule_source`/`reason`/`finding`）vs `audit_result.schema.json`（`issueId`/`decision`/…）vs `11-html-delivery-spec.md` §13.1（`findings`/`ruleEvidence`/…）。交付校验器必填项只能由 router 现场编。
- **动作**：在 `11-html-delivery-spec.md` 补一段叶子字段 → AuditResult 字段映射表。

## P1 · 契约字段无接口对端（review P1-8）

- `request_kind`/`recursive`/`required` 三个字段 `crwu-dws` 全程不用（只用「路径尾随 `/`」约定 + manifest `requestKind`）。
- **动作**：统一叶子装配表与 `crwu-dws` manifest 的字段口径，避免双写漂移。

## P2 · 杂项（review P2-10 ~ P2-18）

| 编号 | 项 | 落点 |
| --- | --- | --- |
| P2-10 | 已下架技能 `rent` 残留 3 处 | `crwu-audit-datacheck/SKILL.md`、`references/00-KB装配表.md` |
| P2-11 | 历史项目名「泰和里」写入「检查维度」 | `crwu-audit-biz-financing-debt/references/02-review-focus.md` |
| P2-12 | 契约编号漂移（写「契约 04」，实际为 `03-审核统计与台账规范`） | `crwu-audit/SKILL.md` |
| P2-13 | `crwu-audit-optimize` 仍用旧画像词（`object_type`/`scenario`/`stage`/`report_type`…），词表落点指向已不存在的 `01-audit-angles-catalog.md` | `crwu-audit-optimize/SKILL.md`、`references/00`、`references/01` |
| P2-14 | 引用不存在的 `docs/design-crwu-dws.md §10 决策点 D8–D12` | `crwu-dws/SKILL.md`、`references/00`、`references/02` |
| P2-15 | 业务叶子历史条数声称「数据驱动」但无来源、不可复现 | 8 个 `crwu-audit-biz-*/references/02-review-focus.md` |
| P2-16 | `docs/CHANGELOG.md` 历史条目写 `02-资产类型/房地产/`（应为 `01-房地产/`） | 已随历史记录，可不动 |
| P2-17 | `crwu-audit-datacheck` references 命名 `00-KB装配表.md` 与治理推荐 `01-kb-assembly.md` 不一致（可豁免，但 router 需兼容两种命名） | `crwu-audit-datacheck/references/` |
| P2-18 | 叶子标题层未把「§一 缺口」与「§二 可用」分开陈述 | 8 个 `crwu-audit-biz-*/references/02-review-focus.md` |

## 知识库侧待办（不在本仓执行，逐项登记）

| 库内路径 | 状态 / 影响 |
| --- | --- |
| `04-监管覆盖/{证券,司法,国资,财务报告,金融国资}` | 每目录仅 README，无审核文件 → overlay 轴落地前置 |
| `01-业务路线/…`（见 P1 业务轴） | 5 个子业务必检项「待补」+ 2 个空文档 |
| `05-风险案例/` | 仅 README，无案例正文 |
| `06-规则库/M-法律法规合规`、`易错点库`、`模板库`、`素材-官方原文` 部分 | 无技能认领 |
| `00-总纲/README` §3 | 引用已不存在的「知识库大纲与进度总表」（优化文档 §2.1 已记 gap） |
| `06-规则库/03-通用准则-程序与档案/机构业务质量控制指南2026-6号` | 索引卡，旧版（〔2017〕46 号）未精编 → 2026 年内机构质控对照无现行依据 |
| `06-规则库/03-通用准则-程序与档案/资产评估执业准则-利用专家工作及相关报告-索引卡` | 索引卡，精编待办 |
| `06-规则库/02-通用准则-报告与披露/资产评估报告准则` | 索引卡，报告准则重构待跟踪 |
| `06-规则库/02-通用准则-报告与披露/报告准则-精编条目` 声明要素条目数 | 规则条目（六项）vs 模板参考（七项）不一致，待核 |

## 落地归口备忘

- **结构/映射类**（建技能、一级根、registry、classification、遗留命名迁移）→ `crwu-audit-skill-maintainer`（`audit`/`create`/`repair`/`remap`，先逐文件方案、确认后执行）。
- **内容/词表/算法类**（频次信号、RULE 内容、词表取值）→ `crwu-audit-optimize`（根因分桶 → 方案 → 确认 → 执行）。
- **知识库正文**（监管覆盖、待补必检项、风险案例、旗舰清单精编）→ 知识库维护侧，本仓不代改。
- 任何一级能力落地后，跑一遍 `check_audit_skill_mappings.py --catalog <最新快照> --max-age-hours 2 --emit-map …` 刷新校准表。
